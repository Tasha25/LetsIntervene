

<div id="error_explanation">
<h2> <%= pluralize_ng(@object.errors.count, 'error')  %>
prohibited this <%= split_name(@object.class.name) %> from being saved:</h2>
<p>There were problems with the following fields </p>
<ul>
<% @object.errors.full_messages.each do |msg| %>
  <li> <%= msg %></li>
  <% end %>
  </ul>
  </div>